export {createIcons};

/**
 * @typedef {Object.<string, string>} Icons A mapping of icon key to icon SVG content.
 */

/**
 * @type {(icons?: Icons) => Icons}
 */
const createIcons = (icons = {}) => icons;
